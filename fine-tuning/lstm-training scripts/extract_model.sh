combine_tessdata -e data/tessdata/tam.traineddata data/model/tam.lstm

#after flag -e the first path takes the pre-existing data it was trained on
#the second path specified the output path for the model
